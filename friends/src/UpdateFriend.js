{/*import React, { Component } from 'react';
import axios from 'axios';

class UpdateFriend extends Component {
  constructor(props) {
    super(props);
    this.state = {
        name: this.props.updateAge,
        age: this.props.updateName,
        email: this.props.updateEmail,
    };
  }

  render() {
      return (
        <form onSubmit={this.props.onUpdate}>
          <label htmlFor="friend-name">Name:
            <input type="text" name="name" id="friend-name" value={name} />
          </label>
          <label htmlFor="friend-age">Age:
            <input type="number" name="age" id="friend-age" value={age} />
          </label>
          <label htmlFor="friend-email">Email:
            <input type="text" name="email" id="friend-email" value={email} />
          </label>
          <button type="submit">UpdateFriend Friend</button>
        </form>
      );
  }  
}
export default UpdateFriend*/}