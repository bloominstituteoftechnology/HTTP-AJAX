import React from 'react';

const Friend = props => {
  return (
    <div>
      <h2>Name: {this.state.friends.name}</h2>
      <h4>Age: {this.state.friends.age}</h4>
    </div>
  );
}

export default Friend;