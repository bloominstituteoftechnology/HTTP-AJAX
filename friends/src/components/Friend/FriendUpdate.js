import React from "react";

const FriendUpdate = ({ match }) => {
        <div className="friend-card">
            <p>This is rendering {match.params.id}</p>
        </div>
}

export default FriendUpdate;