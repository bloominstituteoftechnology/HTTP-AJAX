import React from 'react';
import 

const friend = () => {
    return (
        <div>
            
        </div>
    );
};

export default friend;

Friend.propTypes = {
    friend.PropTypes.arrayOf(),
}