import styled from 'styled-components';

const ListContainer = styled.div`
    display: flex;
    flex-wrap: wrap;
`

const CardContainer = styled.div`
    width: 45%;
    margin: 20px 30px;
    border-radius: 4px;
`

export { CardContainer, ListContainer }